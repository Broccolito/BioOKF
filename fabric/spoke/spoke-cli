#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

set -a
# shellcheck source=/dev/null
source "$ROOT/.env"
set +a

cd "$ROOT"
exec "$ROOT/bin/spoke-cli" "$@"
