---
type: Dataset
identifier: DrugBank
subtype: drug-database
xref: [infores:drugbank]
description: Curated database of drugs and drug targets.
---

# DrugBank

An **external-reference source node**: cited via the `infores:drugbank` CURIE in `xref`, with no
`raw_source` (it is referenced, not ingested). It is the `primary_source` for the `binds` edge.
