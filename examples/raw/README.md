# raw/ — immutable source material

Placeholder stand-ins for the ingested sources of this demo bundle. In a real bundle these are
the original bytes (PDF, CSV, image, slide, tweet) saved verbatim and never edited. Each is
referenced by a source node's `raw_source` field.
